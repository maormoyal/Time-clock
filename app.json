{
  "name": "time-clock-server",
  "scripts": {
    "postdeploy": "npm install && npm run build"
  },
  "env": {
    "NODE_ENV": "production"
  },
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ]
}
